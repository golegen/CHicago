language: c
script:
 - make
 - make -C toolchain/chir
before_install:
 - sudo apt-get -qq update
 - sudo apt-get install -y build-essential bison flex libgmp3-dev libmpc-dev libmpfr-dev texinfo xorriso
