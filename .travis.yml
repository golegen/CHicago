language: c
script:
 - BUILD_CORES=3 travis_wait 60 make
 - make -C toolchain/chir
before_install:
 - sudo apt-get -qq update
 - sudo apt-get install -y build-essential bison flex libgmp3-dev libmpc-dev libmpfr-dev texinfo autoconf automake xorriso
